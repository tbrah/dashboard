import { CheckArrayPipe } from './check-array.pipe';

describe('CheckArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
